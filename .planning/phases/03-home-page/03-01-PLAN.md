---
phase: 03-home-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/AnimatedSection.tsx
  - client/src/components/HeroSection.tsx
  - client/src/components/MatchesSection.tsx
  - package.json
autonomous: true
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-07

must_haves:
  truths:
    - "Hero section shows a full-viewport team photo with club crest overlay and CTA button"
    - "Matches section displays a prominent next-match card with live countdown timer (days, hours, minutes, seconds)"
    - "Three recent result score cards show team logos, score, and date with win/loss/draw color indicator"
    - "Matches section has a 'View all fixtures' link pointing to /activities"
    - "An AnimatedSection wrapper exists for reusable scroll-triggered fade-up animations"
  artifacts:
    - path: "client/src/components/AnimatedSection.tsx"
      provides: "Reusable motion.section wrapper with whileInView fade-up"
      exports: ["default"]
    - path: "client/src/components/HeroSection.tsx"
      provides: "Redesigned hero with team photo, crest overlay, gradient, CTA"
    - path: "client/src/components/MatchesSection.tsx"
      provides: "Combined next-match card + recent results cards + view-all link"
      exports: ["default"]
  key_links:
    - from: "client/src/components/MatchesSection.tsx"
      to: "useActivities hook"
      via: "useActivities({ params: { type: 'match' } }) for recent results"
      pattern: "useActivities"
    - from: "client/src/components/AnimatedSection.tsx"
      to: "motion/react"
      via: "motion.section with whileInView"
      pattern: "motion\\.section"
---

<objective>
Install the motion library, create a reusable AnimatedSection component, redesign the HeroSection, and build a new combined MatchesSection with next-match countdown and recent results cards.

Purpose: Establish the hero visual impact and match-day experience that makes visitors feel they are on a real football club's website.
Output: AnimatedSection.tsx, redesigned HeroSection.tsx, new MatchesSection.tsx, motion installed.
</objective>

<execution_context>
@/Users/aungkaungmyat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aungkaungmyat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-home-page/03-CONTEXT.md
@.planning/phases/03-home-page/03-RESEARCH.md

@client/src/pages/home/Home.tsx
@client/src/components/HeroSection.tsx
@client/src/components/NextMatch.tsx
@client/src/hooks/useActivities.ts
@client/src/types/index.d.ts

<interfaces>
<!-- Key types the executor needs -->

From client/src/types/index.d.ts:
```typescript
export interface Activity {
  _id: string;
  title: string;
  description?: string;
  type: ActivityType;
  date: string;
  time?: string;
  location: string;
  opponent?: string;
  result?: string;
  isNextMatch?: boolean;
  homeTeamLogoUrl?: string;
  opponentTeamLogoUrl?: string;
  createdAt: string;
  updatedAt: string;
}
```

From client/src/hooks/useActivities.ts:
```typescript
// Fetches activities with optional params (type, isNextMatch, etc.)
// Returns: { activities, activitiesLoading, activitiesError, nextMatch, nextMatchLoading, nextMatchError }
const useActivities = (options?: { params?: { type?: string; isNextMatch?: boolean }; enabled?: boolean }) => UseActivitiesResult;
```

From existing NextMatch.tsx countdown pattern:
```typescript
// Countdown uses useMemo for targetDateTime + useEffect/setInterval that recalculates from Date.now() each tick
// This pattern is already working -- reuse it in the new MatchesSection
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install motion and create AnimatedSection + redesign HeroSection</name>
  <files>
    package.json
    client/src/components/AnimatedSection.tsx
    client/src/components/HeroSection.tsx
  </files>
  <action>
1. Install the motion package:
   ```bash
   npm install motion
   ```

2. Create `client/src/components/AnimatedSection.tsx` -- a reusable wrapper component:
   - Import `motion` from `"motion/react"`
   - Props: `children: React.ReactNode`, `className?: string`, `delay?: number`
   - Renders `motion.section` with:
     - `initial={{ opacity: 0, y: 30 }}`
     - `whileInView={{ opacity: 1, y: 0 }}`
     - `viewport={{ once: true, amount: 0.2 }}`
     - `transition={{ duration: 0.6, delay, ease: "easeOut" }}`
   - Export as default

3. Redesign `client/src/components/HeroSection.tsx`:
   - Remove the Swiper carousel entirely (it's a home page hero, not a slideshow)
   - Use a single full-viewport hero image (`/zinme-group-photo.jpg`) as background
   - Add a dark gradient overlay (`bg-gradient-to-b from-black/60 via-black/30 to-primary-dark`)
   - Center the club crest (`/ZMUTD Official.png`) prominently at the top of the content area, ~80-100px wide
   - Below crest: club name "ZINME UNITED FC" in uppercase tracking-wider
   - Below that: a bold headline like "Welcome to the Home of Zinme United"
   - Below headline: a subtitle line
   - CTA button: "View Squad" linking to `/players`, styled with `bg-accent text-primary` rounded-full
   - Height: `h-screen` (full viewport)
   - Remove the `nextMatch` floating banner (next match moves to MatchesSection)
   - Remove `slides` prop entirely -- this is now a static hero
   - Props become just `{}` (no props needed) or keep minimal optional overrides
   - Responsive: On mobile, crest ~60px, smaller text sizes
   - Keep the Swiper/swiper CSS imports removed since we no longer use them here

Note: Do NOT remove Swiper from package.json -- it may be used elsewhere. Just remove the imports from this file.
  </action>
  <verify>
    <automated>cd "/Users/aungkaungmyat/Desktop/zinme united/zinme-united-official-website" && npx tsc --noEmit --project client/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>motion is installed, AnimatedSection.tsx exists with motion.section whileInView animation, HeroSection.tsx shows a full-viewport static hero with club crest, gradient overlay, headline, and CTA button (no carousel, no floating next-match banner)</done>
</task>

<task type="auto">
  <name>Task 2: Build MatchesSection with next-match countdown and recent results</name>
  <files>
    client/src/components/MatchesSection.tsx
  </files>
  <action>
Create `client/src/components/MatchesSection.tsx` -- a combined section for fixtures and results per user decision:

**Data fetching within the component:**
- Call `useActivities({ params: { isNextMatch: true }, enabled: true })` to get `nextMatch`, `nextMatchLoading`, `nextMatchError`
- Call `useActivities({ params: { type: "match" }, enabled: true })` to get all match activities
- Filter matches client-side: `activities?.filter(m => m.result).sort((a, b) => new Date(b.date).getTime() - new Date(a.date).getTime()).slice(0, 3)` for recent results

**Section layout:**
- Section header with subtitle "Matchday" and title "Fixtures & Results" using the existing convention:
  ```
  <p class="text-xs font-bold uppercase tracking-[0.2em] text-accent mb-1">Matchday</p>
  <h2 class="text-3xl md:text-4xl font-extrabold text-primary">Fixtures & Results</h2>
  ```
- "View all fixtures" link to `/activities` on the right side (hidden sm:flex pattern)

**Next Match Card (top of section):**
- Reuse the countdown logic from the existing `NextMatch.tsx` (the `useState`/`useEffect`/`setInterval` pattern that recalculates from `Date.now()`)
- Card with gradient background (`bg-gradient-to-br from-primary-dark via-primary to-primary-light`)
- Show: "Next Match" label, home team logo (default: `/ZMUTD Official.png`) VS opponent logo (use placehold.co fallback like existing NextMatch.tsx), date formatted with `date-fns format`, time, location with MapPin icon, countdown grid (days/hours/mins/secs in glassmorphism boxes)
- Handle no-next-match state: show "No upcoming match scheduled" message
- Handle error state: show error message in red

**Recent Results Cards (below next match):**
- 3 horizontal cards in a responsive grid (`grid-cols-1 md:grid-cols-3 gap-4`)
- Each card shows:
  - Home team logo (left) - use `activity.homeTeamLogoUrl` or `/ZMUTD Official.png`
  - Score/result in the center - display `activity.result` as-is (string format unknown, display raw)
  - Opponent team logo (right) - use `activity.opponentTeamLogoUrl` or placehold.co fallback
  - Date below score formatted with `date-fns format(new Date(activity.date), "MMM dd, yyyy")`
  - Opponent name below their logo
- Win/loss/draw color accent: parse result string -- if contains numbers like "3-1" where first > second = green border-left, first < second = red border-left, equal = gray. If unparseable, use neutral gray.
- Empty state: if no results exist, show "No recent results" message

**Loading states:** Each sub-section handles its own loading independently (skeleton or simple spinner). Do NOT block the entire section.

**Mobile "View all fixtures" link** at bottom for sm:hidden.

Import `date-fns` `format` for date formatting. Import types from `../types`. Import `useActivities` from `../hooks/useActivities`. Import `Calendar`, `MapPin`, `Clock`, `ChevronRight` from `lucide-react`. Import `Link` from `react-router`.
  </action>
  <verify>
    <automated>cd "/Users/aungkaungmyat/Desktop/zinme united/zinme-united-official-website" && npx tsc --noEmit --project client/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>MatchesSection.tsx exists with: (1) next-match card showing opponent logos, date, venue, and live countdown timer, (2) 3 recent result score cards with team logos and scores, (3) "View all fixtures" link to /activities, (4) independent loading states per sub-section</done>
</task>

</tasks>

<verification>
- `npm ls motion` confirms motion is installed
- TypeScript compilation passes with no errors
- AnimatedSection.tsx exports a default component using motion.section with whileInView
- HeroSection.tsx renders a full-viewport static hero (no Swiper imports)
- MatchesSection.tsx fetches data via useActivities and renders next match + recent results
</verification>

<success_criteria>
- motion package installed in package.json
- AnimatedSection provides reusable scroll-triggered animation wrapper
- HeroSection shows team photo with club crest overlay and CTA (no carousel)
- MatchesSection displays next-match countdown card and 3 recent result cards
- All "View all" links point to correct existing routes (/activities)
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/03-home-page/03-01-SUMMARY.md`
</output>
