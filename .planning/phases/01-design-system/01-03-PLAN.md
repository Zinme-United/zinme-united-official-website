---
phase: 01-design-system
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - client/src/layouts/AdminLayout.tsx
  - client/src/pages/activities/Activities.tsx
  - client/src/pages/activity-management/ActivityManagementPage.tsx
  - client/src/pages/articles-details/ArticlesDetails.tsx
  - client/src/pages/articles/ArticlesPage.tsx
  - client/src/pages/club-management/ClubManagementPage.tsx
  - client/src/pages/dashboard/AdminDashboard.tsx
  - client/src/pages/gallery-management/GalleryManagementPage.tsx
  - client/src/pages/home/Home.tsx
  - client/src/pages/news-management/NewsManagementPage.tsx
  - client/src/pages/our-club/ClubPage.tsx
  - client/src/pages/player-details/PlayerDetails.tsx
  - client/src/pages/player-management/PlayerManagementPage.tsx
  - client/src/pages/players/Players.tsx
  - client/src/pages/unauthorized/UnauthorizedPage.tsx
autonomous: true
requirements:
  - DSGN-01

must_haves:
  truths:
    - "No hardcoded hex color values remain in any page or layout file"
    - "All pages use semantic Tailwind token classes (bg-primary, text-accent, etc.) instead of arbitrary value syntax"
    - "Players.tsx gradient uses token classes or var() references instead of raw hex"
    - "PlayerDetails.tsx chart/SVG colors are documented with token comments where hex must stay"
    - "Pages render visually identical to before (same colors, just via tokens)"
  artifacts:
    - path: "client/src/pages/**/*.tsx"
      provides: "All 14 page files migrated to semantic token classes"
    - path: "client/src/layouts/AdminLayout.tsx"
      provides: "Layout file migrated to semantic token classes"
  key_links:
    - from: "client/src/pages/**/*.tsx"
      to: "client/src/index.css @theme tokens"
      via: "Tailwind utility classes"
      pattern: "bg-primary|text-primary|text-accent|bg-surface"
---

<objective>
Migrate all 15 page and layout files from hardcoded hex color values to semantic Tailwind token classes.

Purpose: Complete the hex-to-token migration for the pages and layouts layer, working in parallel with Plan 02 (components layer).
Output: Every page and layout file uses token classes exclusively -- zero hardcoded hex values remain across the entire client/src/ directory.
</objective>

<execution_context>
@/Users/aungkaungmyat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aungkaungmyat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-system/01-CONTEXT.md
@.planning/phases/01-design-system/01-RESEARCH.md
@.planning/phases/01-design-system/01-01-SUMMARY.md

<interfaces>
<!-- Token classes available from Plan 01's @theme block in index.css -->

Replacement map (Tailwind arbitrary value -> semantic token class):
  bg-[#003b75]       -> bg-primary
  text-[#003b75]     -> text-primary
  hover:bg-[#003b75] -> hover:bg-primary
  border-[#003b75]   -> border-primary
  text-[#0056b3]     -> text-primary-light
  hover:text-[#0056b3] -> hover:text-primary-light
  bg-[#0056b3]       -> bg-primary-light
  text-[#FFD700]     -> text-accent
  text-[#ffd700]     -> text-accent
  bg-[#ffd700]       -> bg-accent
  bg-[#f8f9fa]       -> bg-surface-alt
  text-[#1a1a2e]     -> text-text
  bg-[#ffffff]       -> bg-surface
  text-[#6b7280]     -> text-text-muted
  border-[#ffd700]   -> border-accent
  from-[#003b75]     -> from-primary
  to-[#003b75]       -> to-primary
  via-[#003b75]      -> via-primary

For inline styles: style={{ color: "var(--color-primary)" }}
For icon color props: Remove color prop, use className="text-primary"

Additional gradient tokens for Players.tsx:
  #001e3a -> from-primary-dark (or from-[#001e3a] with comment /* primary-darker gradient stop */)
  #0a7abf -> to-primary-light (close enough, or keep as from-[#0a7abf] with comment)
  #0b4e8a -> bg-primary (close match) or keep with comment

For ArticlesPage.tsx featured card:
  #0b4e8a -> bg-primary-dark or bg-primary with adjusted opacity
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate management pages and admin layout (high hex count files)</name>
  <files>
    client/src/pages/our-club/ClubPage.tsx,
    client/src/pages/activity-management/ActivityManagementPage.tsx,
    client/src/pages/player-management/PlayerManagementPage.tsx,
    client/src/pages/news-management/NewsManagementPage.tsx,
    client/src/pages/gallery-management/GalleryManagementPage.tsx,
    client/src/pages/club-management/ClubManagementPage.tsx,
    client/src/pages/articles/ArticlesPage.tsx,
    client/src/pages/articles-details/ArticlesDetails.tsx,
    client/src/layouts/AdminLayout.tsx
  </files>
  <action>
Open each file and replace ALL hardcoded hex color values with semantic token classes.

**Systematic approach for each file:**
1. Find all `[#` patterns in className strings -- replace with token class equivalents per the mapping
2. Find all `color="#` or `color: "#` in props/inline styles -- replace with className or var() approach
3. Find all `style={{ ... "#` patterns -- replace hex with `var(--color-*)` references
4. Find all `focus:ring-[#...]`, `focus:border-[#...]` patterns -- replace with token versions

**Special handling for ArticlesPage.tsx:**
- Has `#0b4e8a` for a featured article background -- replace with `bg-primary-dark` (close semantic match) or `bg-primary` with adjusted opacity. The key is using a token, not a hardcoded value.

**Management pages pattern:** These pages heavily use `#003b75` for buttons, headers, borders, and table accents. The replacement is straightforward: all map to `primary` variants.

After each file, grep to confirm zero hex values remain.
  </action>
  <verify>
Run: `cd client && grep -rn "#003b75\|#0056b3\|#ffd700\|#FFD700\|#f8f9fa\|#0D5BD7\|#0b4e8a\|#001e3a\|#0a7abf" src/pages/our-club/ src/pages/activity-management/ src/pages/player-management/ src/pages/news-management/ src/pages/gallery-management/ src/pages/club-management/ src/pages/articles/ src/pages/articles-details/ src/layouts/` -- should return empty.
  </verify>
  <done>
All 9 management/admin files contain zero hardcoded hex color values. All colors reference semantic token classes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrate public pages and special cases (Players, PlayerDetails, Home)</name>
  <files>
    client/src/pages/players/Players.tsx,
    client/src/pages/player-details/PlayerDetails.tsx,
    client/src/pages/home/Home.tsx,
    client/src/pages/dashboard/AdminDashboard.tsx,
    client/src/pages/activities/Activities.tsx,
    client/src/pages/unauthorized/UnauthorizedPage.tsx
  </files>
  <action>
**Players.tsx (special -- has gradient hex values):**
- Replace standard `#003b75` references with `bg-primary`, `text-primary` etc.
- For gradient stops `#001e3a` and `#0a7abf`: replace `from-[#001e3a]` with `from-primary-dark` and `to-[#0a7abf]` with `to-primary-light`. These tokens exist in the @theme block. If exact shades are needed that don't match tokens, use the closest token (the visual difference is minimal and consistency matters more).

**PlayerDetails.tsx (special -- has chart/SVG color configs):**
- Replace standard className hex values with token classes
- For chart config objects that pass colors as JS strings (like `"#0D5BD7"` for chart backgrounds): use `var(--color-primary-light)` in style objects where possible, or keep as hardcoded string with a comment `/* token: --color-primary-light */`
- Remove the commented-out react-icons import line if Plan 01 Task 2 hasn't already done so
- Chart-specific colors that are NOT club colors (greens, etc.) can stay as-is

**Home.tsx, AdminDashboard.tsx, Activities.tsx, UnauthorizedPage.tsx:**
- Straightforward replacements following the standard mapping

After all files, run a FULL audit grep across the entire client/src/ directory (excluding index.css which was handled in Plan 01) to confirm zero theme hex values remain anywhere.
  </action>
  <verify>
Run the comprehensive audit: `cd client && grep -rn "#003b75\|#0056b3\|#ffd700\|#FFD700\|#f8f9fa\|#0D5BD7\|#0b4e8a\|#001e3a\|#0a7abf" src/ --include="*.tsx" --include="*.ts"` -- should return empty or only documented SVG/chart exceptions with token comments. Then run `npm run build 2>&1 | tail -5` to confirm no build errors.
  </verify>
  <done>
All 6 page files contain zero hardcoded hex color values (except documented chart/SVG exceptions in PlayerDetails.tsx with token comments). Full codebase hex audit passes. App builds successfully. DSGN-01 requirement is fully satisfied across all files.
  </done>
</task>

</tasks>

<verification>
1. `cd client && grep -rn "#003b75\|#0056b3\|#ffd700\|#FFD700\|#f8f9fa\|#1a1a2e\|#0D5BD7\|#0b4e8a\|#001e3a\|#0a7abf" src/ --include="*.tsx" --include="*.ts"` returns empty or only documented exceptions
2. `cd client && npm run build` completes without errors
3. Combined with Plan 02 results, the ENTIRE codebase is free of hardcoded hex theme colors
</verification>

<success_criteria>
- Zero hardcoded hex color values in client/src/pages/ and client/src/layouts/ (excluding documented chart exceptions)
- All pages use semantic token classes from the @theme system
- App builds without errors after migration
- Full codebase audit confirms DSGN-01 is satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system/01-03-SUMMARY.md`
</output>
